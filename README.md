# Git 规范提交工具

> [!IMPORTANT]
> 只能用来 commit

## 配置

```toml
# 是否开启 emoji
emoji = true
# 是否严格使用配置内的 scope
# true  = 只能选择，不能自定义
# false = 可选择 + 可自定义
strict_scope = true
# 简短描述最大长度（默认 100）
max_header_length = 100
```
